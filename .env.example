# Firebase configuration is in src/config/firebase.ts
# This project uses Firebase Authentication and Firestore

# Optional: Google OAuth for Calendar Integration
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# Optional: Use Firebase Emulator for local development
VITE_USE_FIREBASE_EMULATOR=false

# Note: Firebase API keys are public and safe to expose
# Security is enforced through Firestore Security Rules
